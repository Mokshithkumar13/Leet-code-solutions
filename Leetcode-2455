class Solution:
    def averageValue(self, nums: List[int]) -> int:
       count=0
       total=0
       for num in nums:
        if num%6==0:
            count+=1
            total+=num
       return total // count if count else 0
